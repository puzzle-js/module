enum SERVICE_TYPE {
  MODULE = "MODULE",
  API = "API",
  FRAGMENT = "FRAGMENT",
  INJECTABLE = "INJECTABLE",
  DATA_PROVIDER = "DATA_PROVIDER",
  RENDER_ENGINE = "RENDER_ENGINE"
}

enum META_TYPES {
  TYPE,
  CONFIGURATION,
  PATH,
  API_HANDLERS,
  FRAGMENT
}

export {
  SERVICE_TYPE,
  META_TYPES
}
